import React from "react";
import { Layout } from "../components/common";

const Resume = () => {
    return (
        <Layout>
            <div>
                <h1>Resume</h1>
            </div>
        </Layout>
    );
};

export default Resume;
