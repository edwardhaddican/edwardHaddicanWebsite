import React from "react";
import { Layout } from "../components/common";

const Projects = () => {
    return (
        <Layout>
            <div>
                <h1>Projects</h1>
            </div>
        </Layout>
    );
};

export default Projects;
