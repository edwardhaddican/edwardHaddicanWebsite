import React from "react";
import { Layout } from "../components/common";

const Lectures = () => {
    return (
        <Layout>
            <div>
                <h1>Lectures</h1>
            </div>
        </Layout>
    );
};

export default Lectures;
